version: '3'

services:
  app:
    container_name: rwanda_admin_app
    build:
      context: .
      dockerfile: Dockerfile
    command: yarn serve
    privileged: true
    volumes:
      - .:/app
    ports:
      - 4000:8080