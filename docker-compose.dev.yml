version: '3'

services:
  rwanda_admin:
    container_name: rwanda_admin
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: yarn serve
    privileged: true
    volumes:
      - .:/app
    ports:
      - 3000:8080
